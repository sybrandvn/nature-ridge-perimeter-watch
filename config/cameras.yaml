# Per-camera fence geometry and overrides.
#
# `order` (fence position): 2026-08-28, user decided against running gate 1's
# inference (`scripts/infer_camera_order.py`) -- no reason to expect the physical
# fence order differs from the numerical/alphabetical camera-id order, so `order`
# is just that sort (cam01=0, cam01a=1, cam01b=2, cam02=3, ... cam16=17). Gate 1
# is therefore resolved without inference; `infer_camera_order.py` is kept for
# reference/validation but is no longer on the critical path.
#
# `fence`, `outside`, `depth_cutoff`, and `ignore` are hand-drawn (Phase 0c) and
# will later be revised by the browser zone editor (Phase 2) — an empty entry with
# just `id`/`aliases`/`order` is valid and means "not yet configured", which
# classify.py treats as ambiguous rather than a crash.
#
# `outside` (`left`/`right`) is plain screen position: for a query point, compare its
# x to the fence's x at that same row (interpolated along the polyline) -- larger x is
# `right`, smaller is `left`. Point order in the polyline doesn't matter (2026-08-30:
# replaced an earlier direction-of-travel/"which hand" convention that inverted this
# for any top-to-bottom-traced fence -- see docs/plan.md Phase 0c step 11 and
# src/zones.py). Per the user, the exterior side of the fence (what `outside` picks
# out) is compass south for this property.
#
# 2026-08-28: flipped left<->right for every camera except cam08 (whose points
# are ordered bottom-to-top, opposite of everyone else, and was independently
# hand-confirmed: "vegetation/outside is to the right"). Open ground/cleared
# area in frame is the INTERIOR (patrolled) side, not the exterior -- the
# earlier assumption had it backwards for the majority.
unknown_camera_id: "unknown"

# Discovered from real backfilled captions (format: "Cam Alert*: ... NATURE RIDGE
# COMPLEX, MOTIONVIEWER <n> @ <date> <time>"). `order` is numerical/alphabetical
# by id (see note above); `fence`/`outside`/`depth_cutoff`/`ignore` are still
# unset for cameras with no spike/label geometry yet, filled in by the browser
# zone editor (Phase 2).
cameras:
  - id: cam01
    # Retired: replaced on-site by cam01a (last cam01 clip 2025-09-04, cam01a's
    # first 2025-09-13). Kept configured (not deleted) so historical clips
    # still resolve geometry by camera_id, but excluded from the live camera
    # list (menus, /map order, patrol timeline) via `retired: true`.
    retired: true
    aliases: ["MOTIONVIEWER 1"]
    order: 0
    # User hand-traced the fence in red over an extracted reference frame;
    # points extracted by color-thresholding the red pixels back out (5
    # y-bands, top to bottom). Open ground is to the left walking first
    # point to last, same orientation as cam05/cam06.
    fence: [[0.4111, 0.1656], [0.3436, 0.374], [0.2748, 0.5823], [0.2059, 0.7906], [0.1381, 0.999]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam01/10560.mp4.
    # `outside: right` re-confirmed via side_name against the same reference
    # points as the top-rail line.
    fence_bottom:
      [[0.454, 0.1], [0.4406, 0.3232], [0.4268, 0.5464], [0.4133, 0.7695], [0.3999, 0.9927]]
    # 2026-09-06: fence_pickets -- one real post traced by the user over the
    # screengrab in data/reports/picket_trace_2026-09-06/, extracted by
    # color-thresholding the drawn red line (same recipe as the fence lines).
    fence_pickets:
      - [[0.3064, 0.5156], [0.3811, 0.9907]]
    metric_calibration: true
  # cam01a: confirmed by the user to be mounted at the exact same physical spot as
  # cam01 -- a straight replacement, not a second camera. Matches the data: cam01's
  # last clip is 2025-09-04, cam01a's first is 2025-09-13. Kept as separate camera
  # ids anyway (not merged) since the backfill/labels/spike pipeline is keyed by
  # camera_id and merging would need every downstream row relabelled.
  # User 2026-09-08: cam01a is the POLE-MOUNTED one, and it reads that pole as
  # very bright; the guard also flashes the camera directly. Between them that
  # is why 33% of its clips are overexposed and 37% carry the blinding flag --
  # the worst on site, and none of it is something anyone can clean. See
  # NO_MAINTENANCE_CAMERAS in scripts/rank_candidates.py.
  # cam01b: a different device, started 2025-09-11 (overlaps cam01a) -- not
  # confirmed same spot as cam01/cam01a. Treat as a distinct position until checked.
  - id: cam01a
    aliases: ["MOTIONVIEWER 1A"]
    order: 1
    # 2026-08-31: confirmed remounted between message_id 21980 (2026-08-05
    # 17:18:31Z, old framing intact) and 21981 (2026-08-05 17:22:15Z, new
    # framing already in place) -- a ~4-minute window, bisected via
    # scripts/visualize_zone.py over a spread of historical clips. Old fence
    # traced by the same red-trace/color-threshold method as cam01 (same
    # physical mount); new fence traced by the user over the middle frame of
    # data/history/cam01a/22635.mp4. (2026-09-05 correction: this clip is
    # actually NIGHT IR footage, not daylight as an earlier note here claimed.)
    zones:
      - # 2026-09-05: RE-TRACED both lines -- msg 17444/17445 (the only daylight
        # clip in this era) turned out to be an outlier angle, confirmed by
        # bisecting 9 clips spread across the whole old era: every night/
        # twilight frame shows a consistent scene (a loose free-hanging cable,
        # and a distinct wire-mesh fence post) that 17444's wider daylight
        # view didn't obviously match. Retraced over data/history/cam01a/
        # 18347.mp4 (2026-01-10, twilight) instead, which matches that
        # consistent framing. `outside: right` re-confirmed.
        fence: [[0.4947, 0.1719], [0.4212, 0.3784], [0.3467, 0.5849], [0.2723, 0.7914], [0.1983, 0.9979]]
        outside: right
        depth_cutoff: 0.05
        fence_bottom:
          [[0.503, 0.1646], [0.488, 0.3729], [0.4727, 0.5812], [0.4578, 0.7896], [0.4426, 0.9979]]
        # 2026-09-06: fence_pickets, traced by the user over the era-1 screengrab
        # in data/reports/picket_trace_2026-09-06/ (data/history/cam01a/18347.mp4).
        fence_pickets:
          - [[0.3573, 0.577], [0.4111, 0.9991]]
        metric_calibration: true
      - effective_from: "2026-08-05T17:22:15Z"
        # 2026-09-05: RE-TRACED -- the old line here was only 2 points (every
        # other camera's top line has >=3) and clearly imprecise. Confirmed
        # no SECOND remount within this era first (message_id 21989, 2026-08-05
        # right after the remount, vs 22635, 2026-08-26 -- framing is
        # pixel-for-pixel identical). Retraced by the user over the same
        # data/history/cam01a/22635.mp4. `outside: right` re-confirmed.
        fence: [[0.4013, 0.174], [0.3309, 0.3797], [0.2597, 0.5854], [0.1887, 0.7911], [0.118, 0.9969]]
        outside: right
        depth_cutoff: 0.05
        # 2026-09-05: fence_bottom, traced by the user over
        # data/history/cam01a/22635.mp4.
        fence_bottom:
          [[0.4233, 0.1042], [0.4167, 0.3268], [0.4102, 0.5495], [0.4037, 0.7721], [0.3972, 0.9948]]
        # 2026-09-06: fence_pickets, traced by the user over the era-2 screengrab
        # in data/reports/picket_trace_2026-09-06/ (data/history/cam01a/22635.mp4).
        fence_pickets:
          - [[0.2713, 0.5739], [0.3732, 0.9938]]
        metric_calibration: true
  # Confirmed by the user 2026-08-28: faces the OPPOSITE way to most perimeter
  # cameras -- open ground in frame is the interior side, not exterior.
  - id: cam01b
    aliases: ["MOTIONVIEWER 1B"]
    order: 2
    # Red-trace/color-threshold method. Reversed mount (see comment above): the
    # polyline runs the opposite diagonal (x increases with y), so open ground
    # -- the interior side here -- comes out as outside: left, not right.
    # Usually illuminated at night by a yard spotlight, same as cam16.
    fence: [[0.2479, 0.1385], [0.3038, 0.3536], [0.3609, 0.5688], [0.4181, 0.7839], [0.4744, 0.999]]
    outside: left
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over a spotlight-illuminated
    # night frame (data/history/cam01b/19985.mp4) -- daylight/twilight footage
    # wasn't clear enough to trace here. `outside: left` re-confirmed via
    # side_name against the same reference points as the top-rail line.
    fence_bottom:
      [[0.2021, 0.2344], [0.2403, 0.4242], [0.2789, 0.6141], [0.3176, 0.8039], [0.3557, 0.9938]]
  - id: cam02
    aliases: ["MOTIONVIEWER 2"]
    order: 3
    # Red-trace/color-threshold method. Open ground to the left, first point to last.
    fence: [[0.4701, 0.1469], [0.3906, 0.3599], [0.3095, 0.5729], [0.2285, 0.7859], [0.1488, 0.999]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam02/8429.mp4.
    fence_bottom:
      [[0.5059, 0.0302], [0.4961, 0.2721], [0.4867, 0.5141], [0.4773, 0.756], [0.468, 0.9979]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab.
    fence_pickets:
      - [[0.3371, 0.53], [0.4552, 0.9928]]
    metric_calibration: true
  - id: cam03
    aliases: ["MOTIONVIEWER 3"]
    order: 4
    # Red-trace/color-threshold method, on a CLAHE-brightened frame (original
    # was too dark to trace confidently). Open ground to the left.
    fence: [[0.4908, 0.1354], [0.3841, 0.3505], [0.2755, 0.5656], [0.167, 0.7807], [0.0606, 0.9958]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam03/8617.mp4.
    fence_bottom:
      [[0.4858, 0.1344], [0.461, 0.3503], [0.4361, 0.5661], [0.4115, 0.782], [0.3867, 0.9979]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab.
    fence_pickets:
      - [[0.3055, 0.5312], [0.3643, 0.9939]]
    metric_calibration: true
  - id: cam04
    aliases: ["MOTIONVIEWER 4"]
    order: 5
    # Red-trace/color-threshold method. Open ground to the left, first point to last.
    fence: [[0.4878, 0.0823], [0.3927, 0.3115], [0.2961, 0.5406], [0.1993, 0.7698], [0.1034, 0.999]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam04/3455.mp4.
    fence_bottom:
      [[0.5, 0.0208], [0.4816, 0.2654], [0.4632, 0.5099], [0.4445, 0.7544], [0.4262, 0.999]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab.
    fence_pickets:
      - [[0.322, 0.4885], [0.4158, 0.9971]]
    metric_calibration: true
  - id: cam05
    aliases: ["MOTIONVIEWER 5"]
    order: 6
    # Drawn from data/history/cam05/4800.mp4 (middle frame). User hand-traced
    # the fence in red over the reference frame with Snipping Tool; these 3
    # points are extracted from that red line (color-thresholded, sampled at
    # top/mid/bottom). Open ground is to the left walking first point to last.
    fence: [[0.4717, 0.2365], [0.3099, 0.6177], [0.1484, 0.999]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam05/8904.mp4.
    fence_bottom:
      [[0.5125, 0.1198], [0.4999, 0.3393], [0.4867, 0.5589], [0.4742, 0.7784], [0.4616, 0.9979]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab.
    fence_pickets:
      - [[0.3579, 0.5218], [0.431, 0.997]]
    metric_calibration: true
  - id: cam06
    aliases: ["MOTIONVIEWER 6"]
    order: 7
    # Drawn from data/history/cam06/21520.mp4 (crawl incident clip). User
    # hand-traced the fence in red over the reference frame with Snipping
    # Tool; these 3 points are extracted from that red line (color-thresholded,
    # sampled at top/mid/bottom). Open background is to the left walking
    # first point to last. This is the TOP RAIL -- unchanged meaning of
    # `fence` for all 18 cameras; do not repurpose this field.
    fence: [[0.4532, 0.1427], [0.3171, 0.5688], [0.1809, 0.9948]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom -- the first bottom/base line traced anywhere
    # in this config. Cameras sit near the top of the fence looking down and
    # slightly outward, so a subject walking INSIDE the fence still projects
    # to the outside of the top-rail line above -- this is why
    # outside_pixel_fraction false-fires on 40%+ of guards regardless of
    # threshold. Traced by the user over data/history/cam06/8467.mp4 (a
    # daylight clip, same camera, framing confirmed unchanged vs the night
    # clip via scripts/visualize_zone.py). Near-vertical because the camera
    # looks down its own fence run, not a tracing error. `outside: right`
    # re-confirmed directly for this line, not re-derived.
    fence_bottom:
      [[0.5028, 0.0688], [0.4992, 0.3003], [0.4953, 0.5318], [0.4914, 0.7633], [0.4875, 0.9948]]
    # 2026-09-05: fence_pickets -- 3 traces of the same real post (a green
    # mesh panel edge, NOT the thin diagonal tensioning wire next to it) at
    # decreasing distance from the camera, over data/history/cam06/8468.mp4
    # (daylight; the original 8467.mp4 trace turned out to be an outlier
    # angle/mistrace, see repo memory). Confirms tilt does NOT taper
    # linearly with depth (measured ratio 0.28 -> 0.20 -> 0.196) -- used to
    # angle-correct pixels-per-metre calibration; see
    # src/zones.py::_picket_direction_at_y.
    fence_pickets:
      - [[0.3453, 0.5711], [0.4649, 0.9969]]
      - [[0.3907, 0.4188], [0.4762, 0.8445]]
      - [[0.4102, 0.366], [0.4832, 0.7392]]
    # 2026-09-05: first camera opted in to src/ground_calibration.py. The two
    # fence lines give a horizon vanishing point and the pickets a vertical
    # one, which fixes focal length (187px, HFOV 81deg) and camera height
    # (2.63m). Validated: camera height stays constant to 1e-15m from 1.3m to
    # 33m, and the guard's flashlight at the far end of the fence reads
    # 28.7-31.7m against the user's independent "20 to 30 metres" estimate
    # (data/reports/metric_calib_2026-09-05/). Capped at 35m: past the far end
    # of the traced base line nothing anchors the ground-plane assumption.
    metric_calibration: true
    metric_max_range_m: 35.0
  - id: cam07
    aliases: ["MOTIONVIEWER 7"]
    order: 8
    # Red-trace/color-threshold method. Open ground to the left, first point to last.
    fence: [[0.4502, 0.1698], [0.3626, 0.3763], [0.2736, 0.5828], [0.1846, 0.7893], [0.0973, 0.9958]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam07/3724.mp4.
    fence_bottom:
      [[0.481, 0.0979], [0.46, 0.3229], [0.4391, 0.5479], [0.4178, 0.7729], [0.3973, 0.9979]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab.
    fence_pickets:
      - [[0.3222, 0.477], [0.3749, 0.9979]]
    metric_calibration: true
  - id: cam08
    aliases: ["MOTIONVIEWER 8"]
    order: 9
    # Worked example (see README.md "How to draw a fence polyline"): fence runs
    # diagonally bottom-left to top-middle in frame; vegetation/outside is to
    # the right of that line walking from the first point to the second. Points
    # are ordered bottom-to-top here, opposite of every other camera's
    # top-to-bottom order, so it's excluded from the 2026-08-28 left/right flip.
    # 2026-08-30: original bottom point (0.03) was off by a wide margin -- it
    # had drifted onto the diagonal cable visible in the same frame instead of
    # the wire mesh. Retraced twice by the user over data/history/cam08/7360.mp4
    # (both traces landed within ~0.01 of each other), re-extracted via the
    # standard red-trace/color-threshold recipe (linear fit through the traced
    # pixels).
    # 2026-08-31: re-traced a third time over data/history/cam08/4292.mp4
    # (clearer fence-top visibility per the user) -- bottom point moved further
    # left again (0.21 -> 0.13), top point unchanged within noise (~0.006).
    # Verified via scripts/visualize_zone.py over the same frame: line tracks
    # the mesh post edge, not the diagonal cable distractor to its left.
    fence: [[0.1282, 0.9958], [0.4409, 0.1854]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam08/4217.mp4.
    fence_bottom:
      [[0.4378, 0.1302], [0.4488, 0.3471], [0.46, 0.5641], [0.471, 0.781], [0.4819, 0.9979]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab -- initially missed on
    # the first extraction pass (a chain of 1-4px anti-aliasing specks along
    # the orange fence_bottom line's own edge nearly matched the same red
    # threshold, but stayed well under the real trace's ~1400px size).
    fence_pickets:
      - [[0.3398, 0.4855], [0.4646, 0.9782]]
    metric_calibration: true
    # 2026-08-31: stationary background light (small greenish point, far down
    # the fence line) confirmed fixed across all 43 frames of
    # data/history/cam08/4055.mp4 -- pixel bbox never left x=[130,139],
    # y=[33,55] out of a 320x240 frame. Box below is that region with a small
    # margin, normalized.
    # 2026-09-03: SECOND stationary light found (data/history/cam08/4306.mp4,
    # a different clip on the same camera) -- pixel bbox held to x=[93,143]
    # y=[15,24] out of a 320x240 frame across all 51 frames, distinct position
    # from the light above. This camera has (at least) two fixed light
    # sources in view.
    ignore:
      - [[0.381, 0.104], [0.462, 0.104], [0.462, 0.263], [0.381, 0.263]]
      - [[0.28, 0.04], [0.46, 0.04], [0.46, 0.12], [0.28, 0.12]]
  - id: cam09
    aliases: ["MOTIONVIEWER 9"]
    order: 10
    # 2026-09-07: confirmed remounted -- user spotted this reviewing the
    # cam06/21520 crawl-incident sweep clip (cam09/21522, 2026-07-21), traced
    # a fresh independent line over data/history/cam09/10979.mp4
    # (2025-01-12) and confirmed by eye it's a real shift, not the same
    # panel's other edge (the agent's own first read of the overlay guessed
    # the latter and was corrected). Dated per the user's own call: in effect
    # from message_id 10979's timestamp onward. Old geometry (no
    # effective_from) unchanged below; do not re-open that question without
    # new evidence.
    zones:
      - # 2026-09-05: RE-TRACED -- the original line (drawn from
        # data/history/cam09/11680.mp4, a heavily vine-overgrown frame) tracked
        # the vine stem, not the wire mesh underneath it. Retraced by the user
        # over data/history/cam09/3501.mp4 (2023-11-25, same clip fence_bottom
        # below came from). `outside: right` re-confirmed via side_name.
        fence: [[0.4948, 0.2198], [0.4262, 0.4133], [0.3566, 0.6068], [0.2871, 0.8003], [0.2186, 0.9938]]
        outside: right
        depth_cutoff: 0.05
        # 2026-09-05: fence_bottom, traced by the user over data/history/cam09/3501.mp4.
        fence_bottom:
          [[0.5041, 0.2042], [0.4938, 0.4026], [0.4831, 0.601], [0.4727, 0.7995], [0.4624, 0.9979]]
        # 2026-09-06: fence_pickets, traced by the user over the
        # data/reports/picket_trace_2026-09-06/ screengrab (data/history/cam09/3501.mp4).
        fence_pickets:
          - [[0.3824, 0.5541], [0.4478, 0.9991]]
        metric_calibration: true
      - effective_from: "2025-01-12T16:22:45Z"
        # 2026-09-07: traced by the user over data/history/cam09/10979.mp4
        # (frame 100/201), the clip the user identified as when the fence
        # moved. `outside: right` carried over unchanged -- same camera
        # orientation, only the fence's own position shifted.
        fence: [[0.4422, 0.0698], [0.3875, 0.3], [0.333, 0.5292], [0.278, 0.7594], [0.2234, 0.9896]]
        outside: right
        depth_cutoff: 0.05
        fence_bottom:
          [[0.4505, 0.0635], [0.439, 0.2969], [0.4273, 0.5292], [0.4158, 0.7625], [0.4045, 0.9958]]
        fence_pickets:
          - [[0.3312, 0.5563], [0.4062, 0.8865]]
        metric_calibration: true
  - id: cam10
    aliases: ["MOTIONVIEWER 10"]
    order: 11
    # Red-trace/color-threshold method. Open ground to the left, first point to last.
    fence: [[0.4495, 0.1979], [0.3666, 0.3974], [0.2821, 0.5969], [0.1977, 0.7964], [0.115, 0.9958]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam10/3576.mp4.
    fence_bottom:
      [[0.4625, 0.1417], [0.4508, 0.3555], [0.4391, 0.5693], [0.4273, 0.7831], [0.4156, 0.9969]]
    # 2026-08-31: stationary background light behind foliage, confirmed fixed
    # (bbox held to roughly x=[117,175] y=[6,63] out of a 320x240 frame) across
    # all 29 frames of data/history/cam10/4049.mp4 -- pixel COUNT flickers as
    # branches sway in front of it, which is what was getting picked up as
    # motion, but the light's own footprint never moves. Box below covers that
    # region with a small margin, normalized.
    ignore:
      - [[0.344, 0.0], [0.569, 0.0], [0.569, 0.292], [0.344, 0.292]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab.
    fence_pickets:
      - [[0.3424, 0.5], [0.3874, 0.9991]]
    metric_calibration: true
  - id: cam11
    aliases: ["MOTIONVIEWER 11"]
    order: 12
    # Red-trace/color-threshold method, on a CLAHE-brightened frame (original
    # was too dark to trace confidently). Open ground to the left.
    fence: [[0.5254, 0.2156], [0.4629, 0.4107], [0.3993, 0.6057], [0.3357, 0.8008], [0.2731, 0.9958]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over a CLAHE-brightened,
    # 4x-upscaled frame from data/history/cam11/4310.mp4 despite the poor
    # source quality (this camera has only 2 clips in the whole corpus) --
    # traced anyway, kept. `outside: right` re-confirmed via side_name against
    # the same reference points as the top-rail line, not re-derived.
    fence_bottom:
      [[0.5369, 0.1844], [0.5255, 0.387], [0.5139, 0.5896], [0.5023, 0.7922], [0.4909, 0.9948]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab, despite the same poor
    # source quality noted above for fence_bottom (only 2 clips in the corpus).
    # A second, much shorter stray mark on the same screengrab (~26px, an
    # accidental short stroke) was discarded rather than kept as a second
    # picket -- too few pixels to fit a reliable line through.
    fence_pickets:
      - [[0.4198, 0.5689], [0.4903, 0.898]]
    metric_calibration: true
  - id: cam12
    aliases: ["MOTIONVIEWER 12"]
    order: 13
    # 2026-09-03: confirmed remounted -- user-traced fence over
    # data/history/cam12/19245.mp4 (2026-03-02) does not match the original
    # trace, but every other sampled clip back through data/history/cam12/
    # 12201.mp4 (2025-02-21) still matches the ORIGINAL fence, so the new
    # position only took effect sometime between those two dates -- no clips
    # exist in between to bisect further. effective_from is set to
    # 19244 (2026-03-02T16:13:54Z), the earlier of the two same-night clips
    # that confirmed the new position (19245 is 19244's Timeout sibling,
    # 2026-03-02T16:18:11Z) -- the earliest timestamp there's real evidence
    # for, not a guessed midpoint of the year-long gap.
    zones:
      - # 2026-09-19: the user retraced all three geometry primitives over
        # data/history/cam12/9162.mp4. Comparison with 8982 (2024-11-19),
        # 9161/9162 (2024-11-23), and the original 3457 trace found no dated
        # camera movement: this is a more accurate trace of the same pose, so
        # it replaces the original geometry for the whole pre-remount era.
        fence:
          [[0.4959, 0.2021], [0.3887, 0.3932], [0.2815, 0.5844], [0.1742, 0.7755], [0.067, 0.9667]]
        outside: right
        depth_cutoff: 0.05
        fence_bottom:
          [[0.5002, 0.2344], [0.487, 0.4255], [0.4737, 0.6167], [0.4605, 0.8078], [0.4472, 0.999]]
        fence_pickets:
          - [[0.3613, 0.4635], [0.4322, 0.999]]
        metric_calibration: true
      - effective_from: "2026-03-02T16:13:54Z"
        fence: [[0.4455, 0.1958], [0.3466, 0.3943], [0.2467, 0.5927], [0.1467, 0.7911], [0.0477, 0.9896]]
        outside: right
        depth_cutoff: 0.05
        # 2026-09-05: fence_bottom, traced by the user over the same clip
        # (data/history/cam12/19245.mp4, 2026-03-02) the new top-rail line
        # itself came from. `outside: right` re-confirmed.
        fence_bottom:
          [[0.4466, 0.1781], [0.4477, 0.3831], [0.4484, 0.588], [0.4492, 0.793], [0.4502, 0.9979]]
        # 2026-09-06: fence_pickets, traced by the user over the era-2 screengrab
        # in data/reports/picket_trace_2026-09-06/ (data/history/cam12/19245.mp4).
        fence_pickets:
          - [[0.3157, 0.4802], [0.398, 0.9905]]
        metric_calibration: true
  - id: cam13
    aliases: ["MOTIONVIEWER 13"]
    order: 14
    # Red-trace/color-threshold method. Open ground to the left, first point to last.
    fence: [[0.4558, 0.1573], [0.357, 0.3669], [0.2566, 0.5766], [0.1561, 0.7862], [0.0574, 0.9958]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam13/3496.mp4.
    fence_bottom:
      [[0.5338, 0.0385], [0.512, 0.2784], [0.4898, 0.5182], [0.4676, 0.7581], [0.4456, 0.9979]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab.
    fence_pickets:
      - [[0.3399, 0.4323], [0.4138, 0.9834]]
    metric_calibration: true
  - id: cam14
    aliases: ["MOTIONVIEWER 14"]
    order: 15
    # Red-trace/color-threshold method, on a CLAHE-brightened frame (original
    # was too dark to trace confidently). Open ground to the left.
    fence: [[0.477, 0.2344], [0.4114, 0.4255], [0.3444, 0.6167], [0.2774, 0.8078], [0.2115, 0.999]]
    outside: right
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam14/3495.mp4.
    fence_bottom:
      [[0.5267, 0.1281], [0.5164, 0.3458], [0.5057, 0.5635], [0.4953, 0.7812], [0.4848, 0.999]]
    # 2026-09-06: fence_pickets, traced by the user over the
    # data/reports/picket_trace_2026-09-06/ screengrab.
    fence_pickets:
      - [[0.3844, 0.5333], [0.4601, 0.9813]]
    metric_calibration: true
  # 2026-08-28: user corrected the earlier "orientation unresolved, not worth
  # resolving" call below -- cam15 does have a traceable fence after all (a
  # clean single line on data/history/cam15/15453.mp4, frame index 5; an
  # earlier frame from the same clip looked like an ambiguous corner/end-post
  # with converging wires, but this one wasn't). Also reversed, same as
  # cam01b/cam16 -- open ground in frame is the interior side, not exterior.
  # Low-risk to have gotten wrong initially: no cam15 clip has ever shown a
  # person.
  - id: cam15
    aliases: ["MOTIONVIEWER 15"]
    order: 16
    # User 2026-09-08: cam15 has never seen a human -- it is very remote. Its
    # near-zero flashlight readings (green-light rule fires on 0.0% of its
    # clips, warmup ratio p90 0.00049) are therefore CORRECT, not the
    # detection failure they look like next to other cameras. Do not "fix"
    # them. Its most-saturated pixels also happen to sit at hue 33.0, exactly
    # green_light_mask's hue_low bound, so any future flashlight there would
    # be marginal -- worth remembering if a human is ever seen on it.
    fence: [[0.3616, 0.0], [0.4024, 0.2497], [0.4437, 0.4995], [0.485, 0.7492], [0.5257, 0.999]]
    outside: left
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over data/history/cam15/18162.mp4.
    # Runs close to parallel with the top rail, offset by ~0.1 in x -- consistent
    # with this camera's close, near-top-down mount on the fence post itself
    # (also a documented wind/storm camera -- cam15/18948-18949). `outside: left`
    # re-confirmed via side_name against the same reference points as the top line.
    fence_bottom:
      [[0.2593, 0.0], [0.3077, 0.2492], [0.3564, 0.4984], [0.4051, 0.7477], [0.4534, 0.9969]]
  # Confirmed by the user 2026-08-28: reversed, same as cam01b -- open ground
  # in frame is the interior side, not exterior. Usually illuminated at night
  # by a yard spotlight, same as cam01b.
  - id: cam16
    aliases: ["MOTIONVIEWER 16"]
    order: 17
    # Drawn from data/history/cam16/3907.mp4 (an earlier, very short/low-frame
    # clip produced a blocky nearest-neighbour upscale that was too rough to
    # trace; this longer clip upscaled with cubic interpolation instead).
    # Reversed mount: polyline runs the opposite diagonal (x increases with
    # y), so open ground -- the interior side here -- comes out as
    # outside: left, not right.
    fence: [[0.5007, 0.1385], [0.5589, 0.3536], [0.618, 0.5688], [0.6771, 0.7839], [0.7355, 0.999]]
    outside: left
    depth_cutoff: 0.05
    # 2026-09-05: fence_bottom, traced by the user over a spotlight-illuminated
    # night frame (data/history/cam16/7582.mp4) -- some glare/haze from an
    # insect near the lens, still usable. `outside: left` re-confirmed via
    # side_name against the same reference points as the top-rail line.
    fence_bottom:
      [[0.4946, 0.0708], [0.5086, 0.3029], [0.5227, 0.5349], [0.5369, 0.7669], [0.551, 0.999]]
